it has authentication and authorization 
 authentication --- any user which visit the website --- the login and signup work is of authentication
(password --- hashing)



Authorisation --- check if the current user has a required access of a functionality or process 


authentication --- bcrypt -- to encrypt a string --- hashing encryption --- hashing 

hashing --- it is conversion of one string to another --by adding buffer(random characters ) ,, so that we can no check the original value 
